apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: biblioteca
spec:
  type: ClusterIP
  selector:
    app: biblioteca-backend
  ports:
    - port: 3001
      targetPort: 3001
